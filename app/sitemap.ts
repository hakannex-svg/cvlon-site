import type { MetadataRoute } from "next"; import { routes, siteConfig } from "@/lib/site-config"; import { isPriceCheckEnabled } from "@/lib/price-check/feature";
export default function sitemap():MetadataRoute.Sitemap{const enabledRoutes=isPriceCheckEnabled()?[...routes,"/price-check"]:routes;return enabledRoutes.map(path=>({url:new URL(path,siteConfig.url).toString(),lastModified:new Date(),changeFrequency:path==="/"?"weekly":"monthly",priority:path==="/"?1:.7}));}
