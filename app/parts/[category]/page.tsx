import { notFound } from "next/navigation"; import { InteriorHero, LinkCards, RfqSection, SectionHeading } from "@/components/Interior"; import { partCategories } from "@/content/site-content"; import { pageMetadata } from "@/lib/metadata";
type Key=keyof typeof partCategories; export function generateStaticParams(){return Object.keys(partCategories).map(category=>({category}));} export async function generateMetadata({params}:{params:Promise<{category:string}>}){const {category}=await params;const d=partCategories[category as Key];return d?pageMetadata(`${d.name} Parts`,d.intro,`/parts/${category}`):{};}
export default async function Page({params}:{params:Promise<{category:string}>}){const {category}=await params;const d=partCategories[category as Key];if(!d)notFound();const path=`/parts/${category}`;return <main><InteriorHero eyebrow="PARTS / CATEGORY" title={`${d.name} parts`} intro={d.intro} path={path} urgent/><section className="section"><div className="shell two-column"><div><SectionHeading label="TYPICAL REQUESTS / 01" title="Component examples"/><ul className="technical-list">{d.examples.map(x=><li key={x}>{x}</li>)}</ul></div><div><SectionHeading label="DOCUMENTATION / 02" title="Details that move the search"/><ul className="technical-list">{d.docs.map(x=><li key={x}>{x}</li>)}</ul></div></div></section><section className="section section-muted"><div className="shell"><SectionHeading label="CONNECTED SUPPORT / 03" title="Beyond the initial search"/><LinkCards items={[{title:"Repair management",body:"Compare repair, exchange and replacement paths where appropriate.",href:"/repair-management"},{title:"AOG support",body:"Escalate an aircraft-on-ground requirement directly to the urgent desk.",href:"/aog-services"},{title:"Supported aircraft",body:"Review core business-aviation platform coverage.",href:"/aircraft"}]}/></div></section><RfqSection sourcePage={path} partCategory={d.category}/></main>}
