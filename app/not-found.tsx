/* eslint-disable @next/next/no-html-link-for-pages -- vinext navigation uses standard anchors. */
import { pageMetadata } from "@/lib/metadata"; import { partSearchHref } from "@/lib/part-search-cta"; import { PUBLIC_CTA } from "@/lib/public-cta"; export const metadata=pageMetadata("Page not found","The requested Civilon page could not be found.","/404");
export default function NotFound(){return <main><section className="interior-hero not-found"><div className="shell"><span className="section-label light">ERROR / 404</span><h1>That page is not on our flight plan.</h1><p>Return to the homepage, explore supported aircraft, or request a part.</p><div className="hero-actions"><a className="button button-primary" href="/">Return home</a><a className="button button-ghost" href={partSearchHref()}>{PUBLIC_CTA.buy}</a></div></div></section></main>}
