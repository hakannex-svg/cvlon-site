import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";
const root=path.resolve(import.meta.dirname,".."),read=(...x)=>fs.readFileSync(path.join(root,...x),"utf8");
test("reported muted text selectors use the approved AA contrast palette",()=>{const css=read("app","globals.css");for(const selector of [".form-kicker",".quick-rfq input::placeholder",".aog-check small",".aog-fields legend",".field-help",".privacy",".section-label",".card-number",".platform-list span",".interior-heading>p,.rfq-layout>div>p,.large-copy"])assert.match(css,new RegExp(selector.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")+"[^}]*color:#(?:52677a|53687b|1765c1|5c7083)"));assert.match(css,/\.urgent-panel \.section-label \{ color:var\(--sky\); \}/);assert.match(css,/\.field-error \{ color:#b4232a; \}/);assert.match(css,/button:focus-visible/)});
test("the utility bar and primary navigation share one banner landmark",()=>{const header=read("components","SiteHeader.tsx");assert.match(header,/return <header><div className="topbar"/);assert.match(header,/<nav className="nav site-nav" aria-label="Main navigation"/);assert.match(header,/<\/nav><\/header>/);assert.equal((header.match(/<header>/g)||[]).length,1)});
